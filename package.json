{
  "name": "fantasia-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter @fantasia/web dev",
    "dev:convex": "bunx convex dev",
    "build": "bun run --filter @fantasia/web build",
    "build:cli": "bun run --filter fantasia-sh build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test",
    "lint": "bun run --filter @fantasia/web lint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
