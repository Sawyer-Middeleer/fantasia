// Legacy config left lying around. The key below is a FAKE example key.
const config = {
  region: 'us-east-1',
  awsAccessKeyId: 'AKIAIOSFODNN7EXAMPLE',
  endpoint: 'https://example.test',
};

module.exports = config;
